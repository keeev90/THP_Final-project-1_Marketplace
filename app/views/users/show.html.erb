<div class='show-user-container'>
  <div class='user-info'>
    <div class='user-info__infos'>
      <h2>Mes informations personnelles</h2>
      <p class='user-first-name'>
        <span class='text-muted'>Prénom : </span>
        <% if @user.first_name && @user.first_name != '' %>
          <%= @user.first_name %>
        <% else %>
          Miaou ? 😿
        <% end %>
      </p>
      <p class='user-last-name'>
        <span class='text-muted'>Nom : </span>
        <% if @user.last_name && @user.last_name != '' %>
          <%= @user.last_name %>
        <% else %>
          Miaou ? 😿
        <% end %>
      </p>
      <p class='user-email'>
        <span class='text-muted'>Adresse mail : </span>
        <%= @user.email %>
      </p>
    </div>
    <div class='user-info__edit'>
      <%= link_to 'Editer mes informations', edit_user_path(@user), class: 'btn' %>
    </div>
  </div>
  <div class='previous-orders'>
    <h2>Mes précédentes commandes</h2>
    
    <% @user.orders.each do |order| %>
    <div class="previous-orders__item"> 
      <p> Commande n°<%=order.id%>
                du <%=format('%02d',order.created_at.day)%>-<%=format('%02d',order.created_at.month)%>-<%=format('%02d',order.created_at.year)%>
                à <%=format('%02d', order.created_at.hour)%>:<%= format('%02d', order.created_at.min) %> </p>
      <p class="previous-orders__item--amount"> Montant : <%=order.total_price%>€ </p>
                <%= link_to "voir les détails", order_path(order), class:"btn info-btn" %>
    </div>
    <% end %>
  </div>
</div>